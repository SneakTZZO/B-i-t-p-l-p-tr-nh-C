/*
	int n;
	char *string;
	printf("%ns", string);
	%ns >> định dạng %s
						+ nếu n <= length(string) >> in xâu bình thường
						+ nếu n > length(string) >> xâu tự động thêm " " vào đầu chuỗi (n - length kí tự)
	Tương tự với
	printf("%nd, n);
*/